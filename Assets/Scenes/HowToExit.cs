using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HowToExit : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject a;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void HowExit()
    {
        a.SetActive(false);
    }
}
